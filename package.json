{
  "name": "reanmate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Bilingual (Khmer/English) study app — React client + Express API.",
  "scripts": {
    "dev": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "migrate": "npm run migrate --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
