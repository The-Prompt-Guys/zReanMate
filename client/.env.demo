# Backend-free UI review: `npm run dev:demo` in client/ (vite --mode demo).
# Vite loads .env.[mode] after .env, so this wins over a local VITE_DEMO=false
# without editing the gitignored client/.env. Committed on purpose — it holds
# no secrets and makes design review repeatable.
VITE_DEMO=true
